# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

.env
.DS_Store
logs/

# Data: 715MB HF snapshot + 3GB of parse output, all reproducible from
# ingestion.py and data_process.py. Never commit it.
Ingestion/data/

# Embedded Qdrant lives under Ingestion/data/, already ignored — noted here
# because the collection is rebuildable from db_populate.py.

# MLflow tracing — the local sqlite backend `tracing.py` defaults to
mlflow.db
mlruns/
